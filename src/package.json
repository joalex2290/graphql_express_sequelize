{
  "name": "graphql_express_sequelize",
  "version": "1.0.0",
  "description": "GraphQL API server using express and sequelize (mysql)",
  "main": "index.js",
  "scripts": {
    "start": "npm install && nodemon --exec babel-node index.js",
    "run": "nodemon --exec babel-node index.js",
    "production": "npm install --production & node index.js"
  },
  "repository": "https://github.com/joalex2290/graphql_express_sequelize.git",
  "author": "John Munoz <john.munoz@microfocus.com>",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "nodemon": "^1.14.11"
  }
}
